import { BackgroundImageDirective } from './background-image.directive';

describe('BackgrdounImageDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundImageDirective();
    expect(directive).toBeTruthy();
  });
});
